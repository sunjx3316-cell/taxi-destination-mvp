# Configuration

Create a local `.env` file before running the server:

```env
AMAP_WEB_KEY=
PUBLIC_BASE_URL=
```

Keep API keys only in the local `.env` file or in CloudBase function environment variables. Do not commit them to GitHub.
